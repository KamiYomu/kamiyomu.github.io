- You need `git` [installed on your machine](https://git-scm.com/downloads)

- Clone the repository

```sh
   git clone https://github.com/KamiYomu/KamiYomu.git
```
- The project folder is in `src` directory.

- The visual studio solution is the file `KamiYomu.Web.sln`